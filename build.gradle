plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm' version '1.4.0-rc'
    id 'com.github.johnrengelman.shadow' version '4.0.4'
}

group = 'io.izzel.taboolib.kotlin'
version = '1.0.18'

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

repositories {
    maven { url "http://repo.ptms.ink/repository/codemc-nms/" }
    maven { url "http://repo.ptms.ink/repository/maven-releases/" }
    mavenCentral()
}

dependencies {
    implementation 'ink.ptms.core:v11600:11600:all'
    implementation 'io.izzel.taboolib:TabooLib:5.44:all'
    implementation 'io.izzel.kether:KetherX:1.0.3'
    implementation "org.jetbrains.kotlin:kotlin-stdlib"
    implementation fileTree(dir: 'libs', includes: ['*.jar'])
}


shadowJar {
    from(sourceSets.main.allSource)
    dependencies {
        it.include it.dependency('io.izzel.kether:.*')
    }
    relocate 'io.izzel.kether', 'io.izzel.taboolib.kotlin.ketherx'
}
